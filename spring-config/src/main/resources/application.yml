server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/pch8388/spring-cloud-study
          search-paths: common-config

management:
  endpoints:
    web:
      exposure:
        include: health,refresh
