server:
  port: 8888
spring:
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: file://${user.home}/working/spring-cloud-config

management:
  endpoints:
    web:
      exposure:
        include: health
